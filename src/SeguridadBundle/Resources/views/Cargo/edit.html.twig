<div class="row">
   <div class="form-group">
      <label class="col-sm-2 control-label required" for="perfil_name">Nombre</label>
      <div class="col-sm-10">{{entity.name}}</div>
   </div>
</div>
<div class="row">
   <div class="col-sm-12">
      {{ form_start(form) }}
      {{ form_row(form.description)}}
      {{ form_row(form.isActive)}}
      <div class="hide">{{ form_rest(form)}}</div>
     {{ form_end(form) }}
   </div>
</div>
